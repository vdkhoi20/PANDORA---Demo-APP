# Public URL of the FastAPI server (server/), reached through the named
# Cloudflare tunnel that runs on the GPU box. See server/run-all.sh for the
# orchestrator that picks free ports and renders ~/.cloudflared/config.yml.
#
# For a one-off demo without a custom domain, fall back to the quick-tunnel
# instructions in server/README.md and paste the printed *.trycloudflare.com URL.
VITE_API_BASE=https://pandora.nguyenvanloc.com
